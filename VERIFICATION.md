# التحقق من حقول التتبع — 2026-09-07

## مراجعة التحكم بالوقود — 2026-09-09

### مقارنة تسجيل المتصفح الجديد

التسجيل الجديد داخل مجلد المشروع يحتوي 30 طلبًا، منها ثلاث دورات أوامر مكتملة:
قطع `S201` مرتين ثم إعادة `S200` مرة واحدة. في كل دورة نجح `ValidPassword`،
وأعاد `SendCommand` رقمًا أكبر من 10، ثم أعاد `GetResponse` ردًا فارغًا يليه
`Success!`، مع HTTP 200.

تم تشغيل مسار إرسال البوت مع محاكاة النقل ومقارنة جميع حقول JSON الناتجة بطلبات
المتصفح الثلاثة، بما فيها مطابقة بيانات الهدف والتحقق دون نشرها؛ تطابقت جميع الحقول.
لم يُعَد إرسال أي طلب تحكم من التسجيل إلى الشبكة.

ثم استُخدمت دالة البوت الفعلية `command_response` لقراءة أرقام الأوامر الثلاثة من
المنصة، وأعادت `Success!` لكل منها. انتهت مهلة الاتصال في المحاولة الأولى عند صفحة
الدخول، ثم نجحت إعادة محاولة القراءة. يستخدم المتصفح `TimeZones=1:00` لقراءة الرد
والبوت `0`؛ لم يغيّر ذلك رد `Success!` في هذه الأوامر الثلاثة.

نجحت اختبارات المشروع الـ123. يثبت هذا توافق الطلبات وقراءة ردود المنصة للقطع
والإعادة؛ لم تُختبر دورة إرسال فعلية جديدة من تيليغرام، ولا يُستنتج من التسجيل وحده
فحص ميكانيكي مستقل للريليه. يبقى اختلاف البوت المقصود هو التحقق من توقف المركبة
وحداثة الموقع قبل القطع؛ إعادة الوقود لا تعتمد على التتبع.

### التسجيل السابق

تسجيل HAR المقدم لاحقًا من المستخدم يؤكد طلب `S201` مع `TrueOrFalse=0`
و`Model=178` و`LoginType=2`. نجح `ValidPassword`، وأعاد `SendCommand` رقم
أمر، ثم أعاد `GetResponse` ردًا فارغًا يليه `Success!`. يطابق هذا مسار الكود؛
لا يتضمن هذا التسجيل إثباتًا منفصلًا لأمر الإعادة أو فحصًا ميكانيكيًا للريليه.
بناءً على طلب المستخدم أصبحت إعادة الوقود تتجاوز فحص التتبع بالكامل، مع بقاء
مطابقة الجهاز وكلمة المرور والتأكيد. شروط التوقف والحداثة أدناه تخص القطع فقط.

أُجريت قراءة مصادَق عليها وفحص `ValidPassword` دون استدعاء `SendCommand`.
ثبت تطابق الجهاز مع الموديل `178` ونوع الجلسة `2`، وقبلت المنصة كلمة المرور.
ملف `JS/Map.js?v=202601519` المشار إليه في إطار الخريطة يربط هذا الموديل
بـ `S201` للقطع و`S200` للإعادة، ويستخدم `GetResponse` لمتابعة الرد بعد القبول.

عند `09:48:21 UTC` أعاد طلب `TimeZones=0` موقعًا بتاريخ `09:19:49`
ووقت خادم `09:46:28`، وحالة `Stop` وسرعة صفر و`isStop=true` و`ACC=0`.
طلب `TimeZones=1:00` أعاد التاريخين نفسيهما بإضافة ساعة، ما يؤكد ضرورة طلب
المنطقة صفر في مسار التحكم. تقدم وقت الخادم منذ الفحص السابق، بينما بقي وقت
الموقع ثابتًا؛ لذلك لا يمكن استخدام وقت الخادم لإثبات حداثة قراءة التوقف.

أضيفت مطابقة الجهاز والمستخدم بين قراءتي الجلسة، والتحقق الصارم من أرقام الموديل
والأمر دون تقريب الكسور، ورفض قوائم الأجهزة وردود الخادم غير الصالحة. زر قراءة
الرد مرتبط بآخر أمر مقبول للمستخدم ويستعلم فقط، حتى عند قدم بيانات الموقع.
تغطي الاختبارات المحلية الإرسال بالمحاكاة، وردود النجاح والانتظار والفشل، وتغير
الجلسة، والتأكيد مرة واحدة، ومنع الطلبات المتزامنة وعدم إعادة الإرسال عند الغموض.

لم يُختبر تشغيل الريليه فعليًا. تفسير `dataContext` يعتمد على تعريف البروتوكول
الموجود بالمشروع، ولا يثبت وحده صحة توصيل الريليه أو استمرار توقف المركبة أثناء
انتظار تنفيذ أمر المنصة. يبقى حد الحداثة 60 ثانية وشرط التوقف وإطفاء ACC كما هو.

## الفحص السابق

أُجري طلب قراءة مصادَق عليه إلى `GetTracking` باستعمال إعدادات المشروع، دون إرسال أوامر إلى الجهاز أو رسائل تيليغرام. اكتمل الطلب عند **12:38:16 UTC**. هذه لقطة وقت الفحص ولا تثبت الحالة الحالية بعده.

| الحقل | القيمة الخام وقت الفحص |
|---|---|
| `isStop` | `1` |
| `stopTimeMinute` | `7` |
| `speed` | `"0.00"` |
| `status` | `"Stop"` |
| `ofl` | `"2"` |
| `distance` | `"11746.6445"` |
| `deviceUtcDate` | `"2026-09-07 13:31:55"` |
| `serverUtcDate` | `"2026-09-07 13:35:25"` |
| منطقة الجهاز المعادة | `"1:00"` |

القيم المحفوظة سابقاً في `gps_output1` تعود إلى 2026-08-17، لذا لا تصلح لتأكيد رسالة المستخدم التي تضمنت مدة 4 دقائق. لا يجوز استنتاج أن حقل التاريخ بتوقيت UTC اعتماداً على اسمه فقط؛ القيم أعلاه معروضة كما أعادها الخادم، ولم تُستخدم لحساب حداثة الموقع آلياً.

## ما ثبت من واجهة المنصة

- [JS/Tracking.js](https://gpscj.net/JS/Tracking.js)، في `GetPopupHtml`، يربط عرض مدة التوقف بقيمة `isStop=1` ويحوّل `stopTimeMinute` بدالة مدة بالدقائق.
- [JS/Map.js](https://gpscj.net/JS/Map.js)، في بناء قائمة الأجهزة، يصنّف `Move` و`Speed` و`Stop` ضمن الأجهزة المتصلة. يصنّف `Offline` و`LoggedOff` و`Arrears` ضمن غير المتصلة أو غير المتاحة.
- الملفان يستخدمان `ofl` كمدة، وبشرط `status=Offline`. لذلك فإن `ofl=2` مع `status=Stop` لا يثبت انقطاع الاتصال. اعتماد الكود القديم على كون `ofl` صفراً كان خطأ.

## المسافة: حد التحقق

القيمة `11746.6445` كانت مصدر عرض `11.75 كم (11,747 م)` إذا افترضنا أنها بالمتر. عملية التقريب والتحويل صحيحة تحت هذا الافتراض، لكن وحدة حقل `GetTracking.distance` ونقطة بداية تجميعه لم تُثبتا.

صفحة التتبع في `JS/Tracking.js` تحسب المسافة الظاهرة من فروق الإحداثيات أثناء التتبع، ولا تستخدم حقل `distance` لهذه القيمة. صفحة قائمة الأجهزة في `JS/Map.js` تعرض حقل مسافة ضمن بيانات القائمة، لكن ذلك لا يكفي لإثبات تطابق وحدته مع استجابة `GetTracking` أو أنه عداد رحلة أو مسافة يومية.

في الفحص الأول عُرضت قراءة `distance` دون وحدة. لاحقاً أكد المستخدم أن الوحدة بالمتر وطلب العرض بالمتر والكيلومتر معاً؛ لذلك أصبح العرض: `11746.6445` متر ← `11.75 كم (11,747 م)`، مع تقريب قيمة المتر في العرض. تبقى المسافات المحسوبة محلياً من الإحداثيات بوحداتها المعروفة. يعرض البوت حالة الحركة باعتبارها آخر بيانات الخادم، وينبّه عند تعارض علامة التوقف مع السرعة أو الحالة.

## التحقق المحلي

نجحت 54 حالة اختبار، منها حالات تؤكد أن `ofl` الموجب لا يجعل `Stop` غير متصل، وأن `ofl=0` لا يجعل `Offline` متصلاً، إضافة إلى عرض مدة التوقف وتحويل المسافة من المتر إلى الكيلومتر بحسب الوحدة التي أكدها المستخدم.

## تصحيح سجل الحركة

ظهر في السجل خطأ HTTP 401 للمسارين القديمين `GetTrack` و`GetNoTrack`. كشفت مراجعة [JS/Playback.js](https://gpscj.net/JS/Playback.js) أن الطلب الصحيح هو `GetDevicesHistory` بمعاملَي `Start` و`End` وإضافة `ShowLBS=2`. الاستجابة تحتوي `devices` و`state` ومؤشري متابعة الصفحات `lastLocationID` و`lastDeviceUtcDate`.

استجاب المسار الصحيح بـHTTP 200. وبعد إصلاح الخدمة، استدعي مسار البوت `_history_work('today')` فعلياً فأعاد **175 نقطة صالحة الإحداثيات** بتاريخ 2026-09-07، دون إرسال رسائل تيليغرام. نجحت **67 حالة اختبار** تشمل المعاملات الصحيحة وتعدد الصفحات ورفض الوصول وحد الطلبات ومنع عرض السجل الجزئي كمكتمل.

## مجدول المهام — 2026-09-11

أضيف مجدول أسبوعي محفوظ في SQLite مع واجهة عربية لاختيار المهمة ووقت البداية
والنهاية والأيام وإعدادات المنطقة أو حدود التنبيه. المثال `18:30 09:00` يشغل
المراقبة مساءً ويوقفها صباح اليوم التالي بحسب `Africa/Casablanca`.

التحقق محلي بمحاكاة Telegram وخدمات GPSCJ: حدود الفترة وعبر منتصف الليل، أيام
البداية وكل الأيام، الأرقام العربية ورفض الأوقات غير الصحيحة، تغييرات إزاحة توقيت
المغرب، حفظ الجداول ومنع تكرار المحاولة، استعادة المراقبة داخل الفترة، إلغاؤها عند
النهاية وسحب الصلاحية، استقلال حالة المنطقة وأزرار الإيقاف، تأكيد الحفظ والتحقق من
ملكية الجدول، وتعطيل أمر الوقود ذي النتيجة غير المؤكدة. لم يُشغّل البوت ولم تُرسل
رسائل أو أوامر فعلية ولم يُنشأ جدول شخصي أثناء التطوير. يتطلب الاستخدام إعادة
تشغيل البوت وإعداد الجدول من قائمته؛ استمرار التشغيل والاتصال شرط لتنفيذ المواعيد.

نجحت **142 حالة اختبار محلية** بعد الإضافة، ونجح فحص ترجمة ملفات Python الثلاثة.

أضيف لاحقًا زر «استخدام الموقع الحالي» لإعداد مركز المنطقة المجدولة من آخر موقع
متاح للجهاز، ثم اختيار نصف القطر بزر أو إدخاله رقميًا. نجحت **146 حالة اختبار**،
تشمل حفظ المركز `(0, 0)`، وفشل الجلب والإحداثيات غير الصالحة مع إتاحة إعادة
المحاولة والإدخال اليدوي، وحدود نصف القطر والأرقام العربية، ومنع الجلب من زر قديم
أو حساب غير مصرح. استُخدمت محاكاة للشبكة ولم تُرسل طلبات فعلية.
